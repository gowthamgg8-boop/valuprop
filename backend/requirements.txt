# valUProp.in — Python dependencies
# Versions chosen for compatibility with Render free tier (Python 3.11)

# Web framework
fastapi==0.110.0
uvicorn[standard]==0.27.1

# Database
sqlalchemy==2.0.25
psycopg2-binary==2.9.9

# HTTP client
httpx==0.27.2

# LLM (Anthropic - Claude) - 0.40+ supports web_search_20250305 tool
anthropic==0.40.0

# Email
resend==0.8.0

# Validation + config (using older pydantic that has prebuilt wheels)
pydantic==2.5.3
python-dotenv==1.0.1

# Payment
razorpay==1.4.1
