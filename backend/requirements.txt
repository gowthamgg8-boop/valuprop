# ValUprop.in — Python dependencies (Phase 3)
# Install: pip install -r requirements.txt

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Database
sqlalchemy==2.0.30
psycopg2-binary==2.9.9    # PostgreSQL driver (SQLite used automatically for local dev)
alembic==1.13.1            # DB migrations

# HTTP client
httpx==0.27.0

# LLM
openai==1.30.1             # GPT-4o-mini (primary)
# anthropic==0.26.0        # Uncomment to use Claude instead

# Email
resend==1.0.0              # Transactional email (3k free/month)

# PDF generation
weasyprint==62.3           # HTML → PDF
# System deps needed for WeasyPrint:
#   Ubuntu:  apt-get install -y libpango-1.0-0 libharfbuzz0b libpangoft2-1.0-0 libfontconfig1
#   macOS:   brew install pango
#   Render:  add build command (see render.yaml)

# AWS (S3 + optional SES)
boto3==1.34.114

# Validation + config
pydantic==2.7.1
pydantic[email]==2.7.1
python-dotenv==1.0.1

# Payment
razorpay==1.4.1

# Rate limiting
slowapi==0.1.9
